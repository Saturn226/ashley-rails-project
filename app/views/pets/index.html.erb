<h1> List of Available Pets </h1>
<%= link_to "load more pets", '#', class: "load_pets" %>
<ul>
  <% @pets.all.each do |pet| %>
   <li> 
    <%= image_tag pet.pet_image.url(:thumb) %>
    <%= link_to pet.name, pet_path(pet)%> 
   </li>
  <% end %>
</ul>


<script>
//what i would like to do is show only 2 more pets at a time whenever
//a show pets button is pressed. How could I do this?
  $(function(){
    $("a.load_pets").on("click", function(e){

    })
  })
</script>